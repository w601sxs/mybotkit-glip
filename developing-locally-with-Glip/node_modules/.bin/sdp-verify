#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sdp-transform/checker.js" "$@"
  ret=$?
else 
  node  "$basedir/../sdp-transform/checker.js" "$@"
  ret=$?
fi
exit $ret
